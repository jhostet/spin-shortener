# TASKS

Tracks the phased build-out described in the approved plan (see conversation/plan history). Phases ship in order; do not start a phase's tasks until the prior phase's tasks are all checked off.

## Phase 1 — Foundations (KV wiring, auth skeleton, minimal link CRUD)

- [x] Declare `links`, `users`, `analytics` KV stores and grant them per component — file(s): spin.toml, runtime-config.toml — done when: `[component.redirect]` has `key_value_stores = ["links", "analytics"]` and `[component.api]` has `key_value_stores = ["links", "users", "analytics"]`, and `spin up --build --runtime-config-file runtime-config.toml` starts without KV-related errors — **note:** contrary to the original plan assumption, Spin does NOT auto-provision named (non-`default`) stores; a `runtime-config.toml` mapping each to `type = "spin"` is required even for local dev, added here and referenced from CLAUDE.md's Commands section
- [x] Add bootstrap-admin and session/cookie Spin variables — file(s): spin.toml — done when: `admin_bootstrap_username`, `admin_bootstrap_password` (secret), `cookie_secure` are declared and wired into `[component.api.variables]`
- [x] Implement real slug lookup + redirect in `redirect` (no password/analytics yet) — file(s): redirect/main.go — done when: `GET /r/{slug}` for an existing `slug:<slug>` KV record returns `302` to `target_url`; missing/disabled slug returns `404`
- [x] Implement local password hashing + verification helpers (Python) — file(s): api/auth.py — done when: a function hashes a password into `pbkdf2_sha256$<iter>$<salt>$<hash>` using stdlib `hashlib`/`hmac`/`secrets` only, and a matching verify function round-trips correctly, verified with a quick script/spike — **note:** `hashlib.pbkdf2_hmac` is not available in componentize-py 0.23.0's WASI CPython; implemented PBKDF2-HMAC-SHA256 manually from `hmac.new`+`hashlib.sha256` instead (confirmed working under `spin up`)
- [x] Implement bootstrap-admin seeding on first run — file(s): api/auth.py, api/app.py — done when: on a fresh KV store, the first request seeds one `user:<username>` admin record from the bootstrap variables, and subsequent runs do not reseed
- [x] Implement `AuthProvider`/`LocalAuthProvider` + session issuance — file(s): api/auth.py — done when: `authenticate(username, password)` returns a `Principal`/`AuthResult` or `None`, and `create_session(username, auth_provider)` writes a `session:<token>` KV record and returns a token
- [x] Implement `POST /api/auth/login`, `POST /api/auth/logout`, `GET /api/auth/me` — file(s): api/app.py, api/auth.py — done when: login sets the session cookie (`HttpOnly`, `Secure` gated by `cookie_secure`, `SameSite=Lax`) and returns `csrf_token`; `me` returns 401 without a valid session and the user/permissions with one; logout clears the session KV record and cookie
- [x] Implement session-validation middleware/helper used by all authenticated endpoints — file(s): api/app.py, api/auth.py — done when: a shared helper resolves the session cookie to a `Principal` or raises/returns 401, re-reading the user record fresh (not cached) each call
- [x] Implement link CRUD (random slug only, no password/custom slug yet) — file(s): api/links.py, api/app.py — done when: `POST/GET/DELETE /api/links` and `GET /api/links/{slug}` work against the `links` KV store, enforce session auth, and enforce owner-or-admin on get/delete
- [x] Build bare-bones `gui`: login, dashboard list, create-link form — file(s): gui/login.html, gui/dashboard.html, gui/index.html, gui/app.js — done when: a user can log in, see their links, and create a new random-slug link, all via `fetch` against `/api/...`
- [x] End-to-end manual verification of Phase 1 — file(s): (none — verification step) — done when: `spin up --build` runs cleanly; a fresh admin can log in via `gui`, create a link, and clicking `/r/{slug}` redirects correctly — verified via a headless-Chromium (Playwright) drive of the real login→dashboard→create→redirect flow, screenshots confirmed, zero console errors. **Note:** local browser testing over plain HTTP requires `SPIN_VARIABLE_COOKIE_SECURE=false` (the session cookie's `Secure` flag otherwise blocks the browser from storing/sending it over non-HTTPS) — production/HTTPS deployments should leave `cookie_secure` at its default `true`.

## Phase 2 — Custom slugs, permissions, password gate, QR

- [x] Add `permissions` list + `links.create_custom_slug` enforcement — file(s): api/auth.py, api/links.py — done when: creating a link with a custom slug returns `403` for a user lacking the permission and succeeds for one who has it (or is `admin`) — verified the admin-bypass and 400/409 paths via curl; the true non-admin-without-permission 403 path was confirmed for real in Phase 5 once user management shipped (created a restricted user via the admin UI, confirmed a live `403`)
- [x] Add custom-slug validation (charset/length) and collision handling — file(s): api/links.py — done when: invalid custom slugs are rejected with a clear error and slug collisions return a conflict error instead of overwriting an existing link
- [x] Add `password_hash` field to link records + `POST /api/links/{slug}/password` — file(s): api/links.py — done when: an owner/admin can set/change/remove a link's password via this endpoint, stored in the shared `pbkdf2_sha256$...` format — also scrubbed `password_hash` from every API response (`create`/`get`/`list`/`set password` now return a `password_protected` boolean instead)
- [x] Implement password-prompt page in `redirect` (embed + html/template) — file(s): redirect/main.go, redirect/prompt.html, redirect/passwordgate.go — done when: `GET /r/{slug}` for a password-protected link renders the prompt instead of redirecting, with the slug safely escaped
- [x] Implement password verification on `POST /r/{slug}` — file(s): redirect/main.go, redirect/passwordgate.go — done when: correct password redirects (`302`), incorrect password re-renders the prompt with an error (`401`), and every request re-fetches the link fresh from KV (no unlock cookie, per the confirmed re-prompt-every-time decision) — verified cross-language (Python-hashed, Go-verified via stdlib `crypto/pbkdf2`) via curl, including that removing a link's password takes effect on the very next request with zero stale state
- [x] Spike: add `qrcode`/`pypng` deps and confirm componentize-py build still works — file(s): api/pyproject.toml — done when: `uv run componentize-py -w spin:up/http-trigger@4.0.0 componentize app -o app.wasm` succeeds with the new deps and the resulting `.wasm` runs under `spin up` — no Pillow/C-extensions pulled in (confirmed via uv.lock: only `qrcode`, `pypng`, `colorama`, all pure Python)
- [x] Implement `GET /api/links/{slug}/qr` (svg + png, web/print sizes, download) — file(s): api/qr.py, api/app.py, spin.toml — done when: both `format=svg` and `format=png` return valid, scannable QR images at both `size=web` and `size=print`, and `download=1` sets `Content-Disposition` — verified under `spin up`; **note:** added a `public_base_url` Spin variable so the QR encodes the short link (`{public_base_url}/r/{slug}`) rather than the raw `target_url` — encoding the target directly would let a scan bypass the redirect, analytics, and password gate entirely
- [x] End-to-end manual verification of Phase 2 — file(s): (none — verification step) — done when: a permitted user creates a custom-slug, password-protected link; visiting it prompts for a password and only redirects on a correct entry; the QR code for the link scans correctly with a phone camera — verified via curl: created a combined custom-slug + password-protected link, confirmed the password prompt/reject/accept flow and a well-formed QR (visually inspected, encodes the short link per source review — no physical phone/camera available in this environment to scan it)

## Phase 3 — Test coverage foundations + time-windowed links

*(a) Testability refactors — must land first, later tasks' tests depend on them*

- [x] Extract `redirect`'s pure logic into `linkgate` — file(s): redirect/linkgate/link.go, redirect/linkgate/password.go, redirect/main.go, redirect/passwordgate.go — done when: `Link`/`ParseLink`/`VerifyPassword` live in package `linkgate` with zero `spin-go-sdk` imports; `main.go`/`passwordgate.go` updated to call into it; `go tool componentize-go build` still succeeds
- [x] Confirm `go test ./linkgate/...` runs clean — file(s): redirect/linkgate/ — done when: a minimal test in the new package passes under plain `go test ./linkgate/...` from `redirect/` — confirmed `go test ./...` (bare) still fails on `package main` with the same `wit_exports.go` error as `go build`, exactly as predicted
- [x] Relocate `Request`/`Response` into `responses.py` — file(s): api/responses.py — done when: both are local dataclasses, `responses.py` has zero `spin_sdk` imports
- [x] Update `auth.py`/`qr.py` to import `Request`/`Response` from `responses` — file(s): api/auth.py, api/qr.py — done when: neither imports anything from `spin_sdk` — also updated `app.py` to import `Request`/`Response` from `responses` (not `spin_sdk.http`) for consistency with what it actually returns, keeping only `Handler` from the real SDK
- [x] Change `ensure_bootstrap_admin` to take resolved credentials — file(s): api/auth.py, api/app.py — done when: signature is `(store, username, password)`, `auth.py` drops `spin_sdk.variables`, `app.py` resolves both variables and passes them, mirroring `_cookie_secure()` — verified `auth.py`/`links.py`/`qr.py`/`responses.py` all import cleanly under host `uv run python3` (previously failed with `ModuleNotFoundError: No module named 'componentize_py_runtime'`)
- [x] Set up pytest tooling — file(s): api/pyproject.toml, api/tests/fakes.py — done when: `uv add --dev pytest` lands in `dependency-groups.dev`, `[tool.pytest.ini_options]` makes `uv run pytest` collect and import cleanly, `FakeStore` is implemented — also added `pytest-asyncio` (`asyncio_mode = "auto"`) since the business logic is all async; writing `test_auth.py` alongside this surfaced a real bug (see below), so this task and the first retroactive-test task effectively landed together
- [x] Verify `spin up --build` still works post-refactor — file(s): (none — verification step) — done when: both components build and login+create+redirect smoke test still passes, confirming the refactor was behavior-preserving — verified via curl: login, custom-slug + password-protected link creation, and the password gate all behave identically post-refactor

*(b) Retroactive tests for existing Phase 1-2 logic*

- [x] Go: `password_test.go` + `link_test.go` — file(s): redirect/linkgate/password_test.go, redirect/linkgate/link_test.go — done when: password verify (correct/wrong/malformed-hash) and link parsing (valid/partial/malformed JSON) cases pass under `go test ./linkgate/...` — Go's `VerifyPassword` was already robust against malformed input (unlike the Python equivalent, see below)
- [x] Python: `test_auth.py` — file(s): api/tests/test_auth.py — done when: hash/verify, LocalAuthProvider, session create/resolve/expire/tamper, CSRF, permission, and bootstrap-admin cases pass under `uv run pytest` — **bug found and fixed**: `verify_password` let `ValueError`/`binascii.Error` propagate on malformed stored values (non-numeric iterations, invalid base64) instead of returning `False`, which would have 500'd instead of cleanly rejecting; now wrapped in try/except with `validate=True` on both b64decode calls
- [x] Python: `test_links.py` (existing-logic subset) — file(s): api/tests/test_links.py — done when: custom-slug/create/get/delete/set-password cases pass — 25 tests, all passing
- [x] Python: `test_qr.py` — file(s): api/tests/test_qr.py — done when: svg/png rendering, format/size validation, download header, and target-URL-encoding-invariant cases pass — 11 tests, all passing (full suite: 60 passed)

*(c) Timed-links feature, tests alongside*

- [x] Go: `start_at`/`end_at` fields + `IsWithinWindow` + `window_test.go` — file(s): redirect/linkgate/link.go, redirect/linkgate/window.go, redirect/linkgate/window_test.go — done when: all boundary cases (no-window, before/at/mid/at-end/after, start-only, end-only, malformed, degenerate) pass — all 13 cases pass (fields were already added to `Link` during the earlier extraction task)
- [x] Go: wire the window check into `handleRedirectGet`/`handleRedirectPost` — file(s): redirect/main.go — done when: both re-check `linkgate.IsWithinWindow` on every request against the fresh KV fetch, 404 identical to nonexistent-slug — `go tool componentize-go build` succeeds
- [x] Python: `start_at`/`end_at` validation on create — file(s): api/responses.py, api/links.py — done when: `parse_iso8601_utc`/`to_iso8601_utc` exist, `POST /api/links` validates/stores per the plan — existing 60-test suite still passes unchanged
- [x] Python: implement `PATCH /api/links/{slug}` — file(s): api/links.py, api/app.py — done when: `handle_update` supports target_url/status/start_at/end_at with presence-checked partial updates and merged-window revalidation, routed in `app.py` — `check_csrf` already treated PATCH as protected, no change needed there; existing 60-test suite still passes unchanged
- [x] Python: tests for window validation + PATCH — file(s): api/tests/test_links.py — done when: all new cases pass — 20 new tests added, full suite now 80 passing
- [x] GUI: create-form window fields + edit view — file(s): gui/dashboard.html, gui/app.js — done when: optional `datetime-local` inputs convert via `toISOString()`, table shows Starts/Expires columns, an edit action PATCHes via a new `api.patch` helper — verified via headless-Chromium drive: created a link with a future `start_at`, confirmed it 404s pre-activation, edited it via the GUI to clear the window, confirmed it then redirects (302) — zero console errors
- [x] End-to-end manual verification of Phase 3 — file(s): (none — verification step) — done when: under `spin up --build --runtime-config-file runtime-config.toml`: a not-yet-active link's `/r/{slug}` returns 404; the same link, once its window opens, redirects 302; a link PATCHed with a past `end_at` returns 404; `go test ./linkgate/...` and `uv run pytest` (from `api/`) both pass cleanly — all verified: `go test ./linkgate/...` 13/13 pass, `uv run pytest` 80/80 pass, and the full not-yet-active→edit→redirects and active→expire-via-PATCH→404 flows confirmed via both a headless-Chromium GUI drive and direct curl
- [x] Update CLAUDE.md with test commands and conventions — file(s): CLAUDE.md — done when: documents `go test ./linkgate/...`/`uv run pytest`, the `go test ./...`-fails gotcha and why, `app.py`'s intentional exclusion from unit tests, the local `Request`/`Response` convention for new code, and the `start_at`/`end_at` semantics

## Phase 4 — Analytics + full gui polish

- [x] Implement hot-path analytics writes in `redirect` (`count`, `events`) — file(s): redirect/main.go, redirect/linkgate/analytics.go, redirect/linkgate/analytics_test.go, spin.toml — done when: each successful redirect updates `count:<slug>` (total + per-day, bounded retention) and writes one `events:<slug>:<slot>` record, with KV errors here never blocking the redirect response — pure logic (`UpdateCount`/`ClassifyUserAgent`/`FormatEvent`/`EventSlot`) unit-tested in `linkgate` (13 new tests, all passing); `analytics_event_slots`/`analytics_day_retention_days` added as Spin variables; analytics only recorded on actual successful redirects, not on 404s or password prompts
- [x] Implement `GET /api/links/{slug}/analytics` aggregation — file(s): api/analytics.py, api/app.py, api/tests/test_analytics.py — done when: it returns `total`, per-day counts, and the recent-events list reconstructed from the `analytics` store, enforcing owner-or-`links.view_all` — 7 new tests, full suite now 87 passing, malformed event entries skipped defensively, events sorted most-recent-first
- [x] Build `gui/links/detail.html` analytics view — file(s): gui/links/detail.html, gui/dashboard.html, gui/app.js — done when: it shows total clicks, a per-day view, and recent events for a given slug — also added a QR preview + SVG/PNG download buttons (part of the original design's GUI intent that was never actually built in Phase 1/2) and a "View" link from the dashboard table; fixed `app.js`'s login-redirect to use an absolute `/login.html` path so it works from the new `gui/links/` subdirectory
- [x] Styling/polish pass across all `gui` screens — file(s): gui/*.html, gui/links/detail.html, gui/vendor/pico.min.css, gui/vendor/NOTICE.txt — done when: all screens use the vendored CSS consistently and are usable by a novice user without instructions — vendored Pico CSS v2.1.1 (MIT) fetched once and committed, not a CDN reference; applied consistently across login/dashboard/detail via semantic `<header>`/`<main class="container">`/`<article>` structure
- [x] End-to-end manual verification of Phase 4 — file(s): (none — verification step) — done when: clicking a link a few times produces visible, correct counts and recent-event entries in the gui's analytics view — verified via headless-Chromium drive (styled login/dashboard/detail screenshots, zero console errors) plus direct curl: click totals always exactly accurate; **finding:** the recent-events ring buffer collides far more than expected (WASI clock resolution, documented in CLAUDE.md's new Analytics section) — improved `EventSlot`'s hash mixing as a partial mitigation, but this is a real, disclosed limitation of the "recent events" feature, not something fully fixed. `go test ./linkgate/...` (26 tests) and `uv run pytest` (87 tests) both pass.

## Phase 5 — Hardening + deploy readiness

- [x] Add username index + `KNOWN_PERMISSIONS` to auth.py — file(s): api/auth.py, api/tests/test_auth.py — done when: `list_usernames`/`add_username`/`remove_username` exist (mirroring `links.py`'s `owner_links` pattern), `ensure_bootstrap_admin` registers the bootstrap admin in the index, no `get_keys()` streaming API needed — 2 new tests, full suite 89 passing
- [x] Implement admin user-management endpoints — file(s): api/users.py, api/tests/test_users.py — done when: `GET/POST/PATCH/DELETE /api/users*` work and are gated on `users.manage` — permission check lives inside each handler (same convention as `links.py`'s custom-slug gate), self-delete/self-disable guardrails added, 26 new tests, full suite now 115 passing
- [x] Wire `/api/users*` routes into app.py — file(s): api/app.py — done when: `GET/POST /api/users` and `GET/PATCH/DELETE /api/users/{username}` are routed, mirroring the existing links routing pattern — full 115-test suite and componentize-py build both still pass
- [x] Build `gui/admin/users.html` — file(s): gui/admin/users.html, gui/dashboard.html — done when: an admin can list, create, edit permissions/role, disable, and delete users through the UI — added a conditional "Manage users" nav link on the dashboard (visible only to admin/`users.manage` holders); verified via headless-Chromium: created a real restricted (non-admin, no-permissions) user through the UI, confirmed the nav link is hidden for them, and confirmed they get real `403`s from both custom-slug link creation and `/api/users` — this closes the Phase 2 follow-up that couldn't be tested until user management existed
- [x] Document accepted security tradeoffs (password brute-force soft-limit, security headers) — file(s): CLAUDE.md — done when: the doc explicitly states these as accepted v1 limitations with rationale — new "Security tradeoffs" section covers brute-force (confirmed via Spin docs that omitting `allowed_outbound_hosts` denies all outbound by default), slug/existence enumerability, missing security headers (a real gap, not a structural one), analytics raciness (cross-referenced), and non-reversible link passwords
- [x] Draft Akamai KV `runtime-config.toml` mapping — file(s): CLAUDE.md — done when: the three named stores (`links`, `users`, `analytics`) are mapped to Akamai's KV provider per their (separately confirmed) runtime-config schema — **major finding, confirmed against Akamai's actual docs**: Akamai Functions only supports a single auto-provisioned `"default"` KV store and uses no `runtime-config.toml` at all — our 3-store architecture isn't deployable there as-is. Per user decision, documented this as a known blocker with the key-prefixing consolidation fix described (single default store, `links:`/`users:`/`analytics:` key prefixes), not implemented preemptively since Akamai deployment isn't an immediate goal
- [x] Update README/CLAUDE.md for the completed feature set — file(s): README.md, CLAUDE.md — done when: both docs accurately describe the shipped functionality and no longer describe stub/scaffold behavior — created a new user-facing root README.md (quick start, feature list); rewrote CLAUDE.md's Project overview/Architecture sections which still described the original empty scaffold
- [x] Write up the SAML/OIDC extension seam — file(s): docs/auth-providers.md — done when: it concretely shows how a redirect-type `AuthProvider` would plug in using the existing `initiate`/`handle_callback` shape, without implementing either provider — covers the `initiate`/`handle_callback` interface, `oauth_state:<state>` KV bookkeeping, JIT user provisioning via `federated_identity:<provider>:<subject>`, and the specific `spin.toml`/`allowed_outbound_hosts` config additions needed, referencing the actual current `auth.py` code
- [x] End-to-end manual verification of Phase 5 — file(s): (none — verification step) — done when: create restricted user via admin UI, log in as them, confirm permission enforcement works for real (closes Phase 2's deferred follow-up check) — verified via two headless-Chromium drives: (1) created a real restricted user, confirmed the "Manage users" nav link is hidden for them and they get live `403`s on custom-slug creation and `/api/users`; (2) exercised edit (grant permission + disable) and delete through the admin UI itself, confirmed a disabled account's login correctly fails with `invalid_credentials`. `go test ./linkgate/...` (26 tests) and `uv run pytest` (115 tests) both pass.

## Bug fixes

- [x] Add custom-slug input to the create-link form — file(s): gui/dashboard.html — done when: `POST /api/links` already supported `custom_slug` (gated on `links.create_custom_slug`) and was exercised via curl/admin-UI-permission-checks in Phase 2/5, but the create form itself never had a field for it, so admins/permitted users had no GUI way to request a custom slug (only random slugs were reachable through the UI) — added a "Custom short link (optional)" input, shown only when `role === "admin"` or `permissions` includes `links.create_custom_slug` (mirrors the existing "Manage users" nav-link gating pattern), wired into the create POST body. Also found and fixed a real bug surfaced while testing this: the input's `pattern="[A-Za-z0-9_-]{3,32}"` threw a console `SyntaxError` in current Chromium, which validates `pattern` using regex "v-mode" (unicode sets) where a bare `-` in a character class must be escaped — fixed to `[A-Za-z0-9_\-]{3,32}`. Verified via headless-Chromium: field visible/usable for admin, hidden for a real restricted (no-permission) user, successful custom-slug creation redirects correctly, and a duplicate-slug submission surfaces the `slug_taken` error — zero console errors after the pattern fix.
- [x] Fix `GET /api/links` ignoring `links.view_all`/admin — file(s): api/links.py, api/tests/test_links.py, gui/dashboard.html — done when: `handle_list` only ever returned the caller's own `owner_links:<username>` slugs, even for admins/`links.view_all` holders — unlike `handle_get`/`handle_update`/`handle_delete`/analytics/qr, which already did the owner-or-view_all check correctly. There was no global slug index to list from, so admins had no GUI route to discover other users' links at all (not even a broken/degraded one) unless they already knew the exact slug. Added an `all_links` KV index (mirrors `auth.list_usernames`'s pattern) maintained in `handle_create`/`handle_delete`, and `handle_list` now serves from it when `role == "admin"` or `links.view_all` is held, else falls back to the owned-slugs index as before. No backfill for links created before this fix — consistent with this repo's existing convention of not adding migration code preemptively (see `list_usernames`/bootstrap-admin precedent). GUI: added an "Owner" column to the links table and switched the "Your links"/"All links" heading based on the same permission check, so an admin's expanded view is legible rather than looking like they own every link. 3 new tests (own-only default, admin-sees-all, view_all-permission-sees-all) plus an index-cleanup assertion added to the existing delete test — full suite 118 passing. Verified live via `spin up --build`: curl'd `/api/links` as admin (both users' links returned) and as a regular user (only their own), then confirmed the same via a headless-Chromium drive of the actual dashboard — zero console errors.

## GUI usability pass

- [x] Add sortable columns + live filter to the links table — file(s): gui/dashboard.html — done when: clicking the Short link/Owner/Destination/Status headers toggles ascending/descending sort (arrow indicator on the active column) and a search box filters rows by slug or destination URL substring as you type, entirely client-side against the already-fetched link list (no refetch per keystroke/click) — verified via headless-Chromium: sort-by-Destination asc/desc both reordered rows correctly with the indicator following the active column, and filtering to "apple" correctly narrowed to the one matching row.
- [x] Tidy the links-table action buttons — file(s): gui/dashboard.html — done when: the previous bare "View" text link + `&nbsp;`-separated Edit/Delete buttons (inconsistent variants, no grouping) are replaced with a single Pico `role="group"` cluster (View rendered as `role="button"` to match Edit/Delete's height/style) — verified visually via a headless-Chromium screenshot.
- [x] Add a shared persistent header/nav across all authenticated GUI pages — file(s): gui/app.js, gui/dashboard.html, gui/links/detail.html, gui/admin/users.html — done when: the Logout button (and whoami/conditional "Manage users" link) no longer disappears when navigating from the dashboard to a link's detail page or the admin users page — previously only dashboard.html had a nav at all. Added `initHeader({backHref, manageUsersHref})` to app.js as the single source of truth, injected into each page's `<header id="app-header">`, returning the fetched principal so each page can still layer its own permission-gated logic (custom-slug field, "All links" heading, etc.) on top — verified via headless-Chromium: logged in, navigated dashboard → link detail → admin/users, confirmed Logout/whoami present and functional on all three.
- [x] Human-friendly permission labels + role-change UX fixes on the admin users page — file(s): gui/admin/users.html — done when: (1) the edit-user form's permission checkboxes showed raw strings like `links.create_custom_slug` instead of friendly text (the create-form already had friendly text hardcoded, just not reused) — added a shared `PERMISSION_LABELS` map driving both forms' checkbox labels and the read-only Permissions column in the users table; (2) the create-form's "Permissions (for role \"user\")" legend never updated when the role `<select>` was changed to Admin — added a `change` listener (and an equivalent per-row `.edit-role` listener for the edit form, keyed off `user.role` at initial render) that updates the legend text; (3) per user decision, when role = Admin the permissions `<fieldset>` is now `disabled` (native HTML disabling — Pico's existing `fieldset[disabled]` CSS greys the checkboxes out for free, no custom styling needed) with the legend reworded to "Permissions (ignored — admins have full access)", while the underlying stored `permissions` value is left untouched (admins already bypass all permission checks via `Principal.has_permission`, so the stored value is inert either way; if later demoted back to "user" they keep whatever was last set rather than starting blank) — verified via headless-Chromium: friendly labels confirmed present on both forms, legend/fieldset-disabled state toggles correctly both directions (user→admin→user) on the create form.
- [x] Compact the create-link form layout — file(s): gui/dashboard.html — done when: the form no longer stacks Destination URL / Custom slug / Starts+Expires as three separate full-width sections — merged the custom-slug field into the existing Starts/Expires `.grid` row (the same optional-fields grouping pattern already used elsewhere in this file), cutting one full-width row of vertical space for admins/permitted users; the grid still collapses cleanly to 2 columns for regular users since the hidden custom-slug label is `display:none` and excluded from grid flow — verified visually via a headless-Chromium screenshot.
- [x] End-to-end manual verification of the GUI usability pass — file(s): (none — verification step) — done when: all of the above are exercised together in one live `spin up --build --runtime-config-file runtime-config.toml` session via headless-Chromium (not just unit-level checks) — confirmed: compact form layout, 3 tidy action-button groups rendered, sort asc/desc + indicator on Destination, filter narrowing to 1 matching row, Logout/whoami present on dashboard/detail/admin pages, friendly permission labels on both admin forms, and the role-change legend/fieldset-disabled toggle working both directions — zero console errors throughout. `uv run pytest` (118 tests) and `go test ./linkgate/...` both still pass (GUI-only change, no backend touched).
- [x] Make Created/Starts/Expires sortable too — file(s): gui/dashboard.html — done when: the sort feature above only covered Short link/Owner/Destination/Status; added the same `class="sortable" data-sort-key="..."` treatment to Created (`created_at`), Starts (`start_at`), Expires (`end_at`) — also fixed the sort comparator to treat `null` (unbounded window) as `""` rather than the literal string `"null"`, which previously would have sorted nulls in a nonsensical spot — verified via headless-Chromium: clicking each of the 3 new headers reorders rows and shows the arrow indicator with no console errors.
- [x] Fix the tidied action buttons breaking out of the table — file(s): gui/dashboard.html — done when: after the previous pass's `role=group` button cluster, the table (now 8 columns wide, several holding full URLs/timestamps) exceeded the page's width and caused page-level horizontal scroll, since this vendored Pico build's `figure{}` rule has no `overflow-x:auto` (`:where(table){width:100%}` only sets a target, not a cap, so the table simply grows past its container with nothing to contain the overflow) — added `#links-figure { overflow-x: auto }` (Pico's own documented "wrap a wide table in a figure" pattern, just not wired up by default in this vendored version) so a too-wide table scrolls within its own contained box instead of blowing out the whole page; also shrank the button-group's Pico spacing variables/font-size further for a visually tighter cluster. Verified via headless-Chromium: `document.documentElement.scrollWidth > clientWidth` is now `false` at a 1280px viewport, and scrolling `#links-figure` itself right reveals a fully clickable Delete button that still deletes the correct link.
- [x] Human-readable error messages for login failures and self-account guardrails — file(s): gui/app.js, gui/login.html, gui/admin/users.html — done when: two flows surfaced raw API error codes verbatim: wrong-password login showed literal `invalid_credentials`, and deleting your own logged-in admin account showed `cannot_delete_self` via a native `alert()` (which reads as a browser dialog, not part of the app). Added a small shared `ERROR_MESSAGES`/`friendlyError(data, fallback)` helper in app.js (covering `invalid_credentials`/`cannot_delete_self`/`cannot_disable_self`, falling back to the caller's default text for anything unmapped rather than ever showing a raw code) and wired it into login's error paragraph, the admin page's edit-user error paragraph, and a new `#users-error` paragraph (styled identically to the app's other inline errors) that replaces the old `alert()` in the delete-user flow. Scoped to just the codes actually reachable from these two flows, not a rewrite of every error message in the app. Verified via headless-Chromium: wrong password shows "Incorrect username or password.", and attempting to delete your own admin account while logged in as it shows "You can't delete your own account while logged in as it." inline in the page (confirmed no `alert()`/dialog fired) instead of the raw error code.
- [x] Break "Expires" onto its own line on the link detail page — file(s): gui/links/detail.html — done when: `Starts`/`Expires` were crammed onto one `&nbsp;`-separated line — split into two separate `<p>` elements, matching the one-fact-per-line style already used for Destination/Status above them — verified visually via a headless-Chromium screenshot.
- [x] End-to-end manual verification of this second GUI pass — file(s): (none — verification step) — done when: all five fixes above are exercised together in one live `spin up --build` session via headless-Chromium — confirmed: Created/Starts/Expires all sort correctly, the table scrolls within its own figure instead of the page (and the scrolled-to Delete button still works), a wrong login password shows the friendly message, self-deleting the admin account shows the friendly inline error with zero native dialogs beyond the expected `confirm()`, and the link detail page shows Starts/Expires on separate lines — zero console errors throughout. `uv run pytest` (118 tests) and `go test ./linkgate/...` both still pass (GUI-only change).

## Visual identity pass

- [x] Replace Pico's default azure palette with a deliberate dark-navy theme — file(s): gui/theme.css (new), gui/login.html, gui/dashboard.html, gui/links/detail.html, gui/admin/users.html — done when: all four GUI pages previously relied on nothing but vendored Pico defaults (`--pico-primary: #0172ad`, i.e. the untouched "azure" preset) with zero custom branding; added `gui/theme.css` (loaded after `vendor/pico.min.css` on every page) overriding Pico's primary/background/muted-color variables with a named token system — `--ss-navy-950`/`--ss-navy-800` (nav chrome), `--ss-slate-500` (secondary text), `--ss-signal-500` (accent, replacing the default cyan-leaning azure with a cooler steel-blue) — plus two shared components: `.slug-chip` (a monospace pill on navy, applied to every rendered short-link/slug so the product's core artifact always reads the same way — dashboard table + link detail heading) and `.status-badge` (functional green/red color coding for a link's `active`/`disabled` status, dashboard table + detail page). Verified via a Playwright-driven `spin up --build --runtime-config-file runtime-config.toml` session (Node's `playwright`, no project browser-automation skill existed yet): logged in, created a test link, screenshotted login/dashboard/detail/admin-users — navy nav, steel-blue buttons/links, slug-chip pills, and the green "Active" badge all rendered correctly on every page, zero console errors.

## Impeccable design-system pass

- [x] Capture product context via `/impeccable init` — file(s): PRODUCT.md (new) — done when: durable product truth (primary users, positioning, operating context, accepted constraints) is recorded in one place instead of re-derived from CLAUDE.md/README.md each time — interviewed the user (marketing/campaign team as primary users; self-hosting driven by avoiding commercial per-seat licensing cost, not data-control; no official Tire Rack brand guide, dark theme is a preference not a locked identity) and wrote `PRODUCT.md` per the tool's canonical schema, explicitly labeling the two unconfirmed sections (evidence-on-hand, accessibility) as absent rather than inventing content.
- [x] Tighten Pico's default type/spacing scale for a denser admin UI — file(s): gui/theme.css — done when: the GUI "feels too big" complaint was root-caused to Pico's default `--pico-font-size` scaling up to 131.25% at wide viewports (tuned for prose/marketing content, not a dense table-and-form admin tool) — pinned `--pico-font-size: 100%`, tightened `--pico-spacing`/form-element spacing, and trimmed `h1`/`h2`/`h3` down from Pico's prose scale; verified by the user via `spin up --build`.
- [x] Record the incumbent visual system via `/impeccable document` — file(s): DESIGN.md (new), .impeccable/design.json (new) — done when: the dark-navy/monospace-data theme that already exists in `theme.css` has a canonical written reference instead of living only as CSS custom properties — scanned `theme.css`/`vendor/pico.min.css`/all four GUI pages and wrote the canonical 8-section DESIGN.md (named North Star "The Signal Room", built off the codebase's own `--ss-signal-500` token) plus the `.impeccable/design.json` sidecar (tonal ramps, 5 component HTML/CSS snippets referencing the real `--ss-*` custom properties). Explicitly documented, rather than silently fixed, one real inconsistency: status badges use `--ss-danger-500` (`#b3261e`) but form/page errors use Pico's inherited `--pico-del-color` (`rgb(136, 56.5, 53)`) — two slightly different "error reds."
- [x] GUI refinement pass via `/impeccable` (general design work, no single named sub-command) — file(s): gui/theme.css, gui/login.html, gui/dashboard.html, gui/admin/users.html, gui/links/detail.html — done when: applied the craft-floor checklist (states, spacing, consistency) across all four pages — consolidated six copies of an inline `style="color: var(--pico-del-color)"` into one shared `.form-error` class; generalized `overflow-x: auto` from dashboard-only to every table wrapper (`admin/users.html`/`links/detail.html` tables could previously overflow off-screen on narrow viewports); added empty-state messaging to the links, users, and both analytics tables (previously a blank `<tbody>`); added a real error message on `links/detail.html`'s failed loads (previously silent); replaced the login page's fixed `margin-top: 8vh` with a flex-centered `.auth-page`/`.auth-shell` pair. One design-hook finding (`broken-image` on `#qr-preview`'s empty `src`) judged and left as a false positive — that image's `src` is populated dynamically per-link by JS, not a missing asset.
- [x] End-to-end verification of the design-system pass — file(s): (none — verification step) — done when: a full `spin up --build --runtime-config-file runtime-config.toml` confirms both components still build clean and the served GUI actually reflects the session's changes — no browser/screenshot tool was available this session, so verification was curl-based: confirmed `200`s on all five touched files and grepped the live responses (not just source) for `auth-page`/`auth-shell`/`.form-error`/`.empty-state`/the tightened `--pico-font-size`/`--pico-spacing` values, plus `/api/auth/me` correctly `401`ing unauthenticated. True visual confirmation (does the login card actually center, do the empty-state rows look right) was left to the user to eyeball directly, since this session had no way to render or screenshot a browser.

## Impeccable critique-and-fix loop

Four rounds of `/impeccable critique` on `gui/` (score trend 20 → 26 → 23 → 26 /40), each followed by a targeted fix pass on the user-selected subset of findings, each verified against a fresh `spin up --build` and both test suites. Snapshots archived under `.impeccable/critique/`. No file(s) column below since each round touched a shifting subset of `gui/dashboard.html`, `gui/admin/users.html`, `gui/links/detail.html`, `gui/login.html`, `gui/app.js`, `gui/theme.css`, and `DESIGN.md` — see the critique snapshots for the authoritative per-round finding list.

- [x] Round 1 fixes (score 20 → 26) — done when: added a delete confirmation to the links table (previously the only destructive action in the app with none), routed link create/edit/delete errors through the existing `friendlyError()`/`ERROR_MESSAGES` pattern instead of leaking raw API codes, and gave link creation a real success payoff (`.form-success` banner: slug chip + Copy button) instead of a silent form-clear.
- [x] Round 2 fixes (score 26 → 23, a real regression this round's own new work introduced) — done when: closed the identical raw-error-code leak on `admin/users.html`'s create-user handler (missed by round 1's narrower scope); added self-serve destination-URL and password set/clear to the dashboard edit row (both already existed server-side, just unreachable from the GUI); surfaced `link.custom`/`link.password_protected` as badges and reformatted timestamps via `Intl.DateTimeFormat` with an "(expiring soon)" cue; standardized delete-confirmation copy between the two destructive flows; added `role="alert"`/`aria-live` to all error/success elements; consolidated two ad-hoc off-ramp font sizes into one documented `caption` DESIGN.md token. The regression: the new `.slug-kind-badge` used Signal Blue as a resting fill (violating DESIGN.md's own hover-only rule) and failed AA contrast, "expiring soon" only covered future dates not already-past ones, and the combined edit-row save could partially succeed with no disclosure.
- [x] Round 3 fixes (score 23 → 26) — done when: recolored the badge off Signal Blue onto neutral slate (fixing both the rule violation and the contrast failure), added a distinct "(expired)" state for already-past `end_at` alongside "(expiring soon)", prefixed the edit-row's partial-save error with "Destination and schedule saved." so a password-only failure is never mistaken for total failure, made the 7 sortable table headers keyboard-operable (`tabindex`/`keydown`/`aria-sort`), and fixed the "Custom slug"/"custom short link" terminology drift on the detail page. This round's own re-critique caught two more issues: the badge recolor fix reused a color pair (Signal Blue text on white, ~4.15:1) that was never fixed at the *button* level, and `role="button"` combined with `aria-sort` on the same `<th>` is an ARIA spec conflict.
- [x] Round 4 fixes — done when: fixed a real correctness bug found only by re-critiquing — the dashboard table's and success banner's slug-chip were live `<a href>` elements, so viewing/copying an existing link fired a real `/r/{slug}` redirect and inflated that link's own click count (the exact metric the product exists to report); converted both to non-navigating `<span>`s and added a dedicated per-row Copy button. Also darkened the app's one accent color (`#2b7fd1` → `#276fb8`, `--pico-primary`/`--ss-signal-500`) so every button and link clears 4.5:1 AA contrast, not just the previously-patched badge; added a neutral "(not yet live)" cue mirroring the expiry treatment for links whose `start_at` is still in the future; made the edit-row's partial-save success actually patch the visible row's cells (not just claim success in text); and dropped the conflicting `role="button"` from the sortable headers, keeping `aria-sort` valid on the default `columnheader` role.

Known remaining drift, not part of any round above: `.impeccable/design.json` (the DESIGN.md sidecar) is now stale across several rounds of token/color changes — worth a `/impeccable document` refresh before it's trusted for anything. *(Resolved by the `/impeccable document` refresh recorded below.)*

## Impeccable audit-and-fix pass

- [x] Refresh the `.impeccable/design.json` sidecar via `/impeccable document` — file(s): .impeccable/design.json — done when: the sidecar, unregenerated since the very first `/impeccable document` run, is brought back in sync with `DESIGN.md`'s accumulated changes across all four critique rounds — updated the `signal-blue` color (and recomputed its tonal ramp) to the AA-passing `#276fb8`, added the `caption` typography step, and added/updated components for the new badges, schedule-state text, and the `.form-success` banner. `DESIGN.md` itself was left untouched (a sidecar-only refresh, per the tool's own "if the user only asks to refresh the sidecar" branch).
- [x] `/impeccable audit gui/` (technical checks: a11y, performance, theming, responsive, implementation integrity) — done when: a dimension-by-dimension audit distinct from the heuristic critique above surfaced issues the critique's lens didn't catch — scored 15/20 initially, found the dashboard/admin edit-row fields lived outside any `<form>` (native `type="url"`/`minlength` validation was inert), the row-action button group's computed height (~28px) was under the touch-target guideline, the links-filter search input had no debounce, a real `<a href>` carried a conflicting `role="button"`, and `DESIGN.md` inaccurately claimed both `dashboard.html` and `admin/users.html` skip their page-level `h1` (only `dashboard.html` actually does).
- [x] Audit fix pass — file(s): gui/dashboard.html, gui/admin/users.html, gui/theme.css, DESIGN.md — done when: wrapped both edit rows in real `<form>` elements (Save as `type="submit"`, handlers reading via `form.querySelector(...)` so native validation and its screen-reader announcements actually fire); bumped the button-group's vertical padding (`0.3rem` → `0.4rem`) for a real, if partial, touch-target improvement (confirmed the AA 24×24px minimum was already met — only the stricter AAA 44px target wasn't, correcting an initial over-broad WCAG citation); debounced the filter input (200ms); added a real `.visually-hidden` label for it; corrected `DESIGN.md`'s inaccurate h1-skip claim. Deliberately left as-is rather than "fixed": the `role="button"` on the View link, since it's load-bearing for Pico's own button-look CSS selector and removing it without replicating that styling surface would risk a real visual regression for a minor ARIA nuance — recorded as an accepted convention in `DESIGN.md` instead. Also deliberately left open: a real narrow-viewport verification pass, since no browser-automation tool has been available in any session so far.
- [x] Re-audit to confirm — done when: independently re-verified all fixes against the actual code (not the fix claims) — score rose to 19/20, zero regressions found, only the narrow-viewport item remains open (a tooling gap, not an effort gap).
- [x] Narrow-viewport verification pass (the item left open above) — file(s): (none yet — audit only, no fix applied) — done when: Playwright (now available in-session) drove a live `spin up --build --runtime-config-file runtime-config.toml` at 375px and 320px viewports across login, dashboard, admin/users, and links/detail — no page-level horizontal overflow anywhere, the `#links-figure`/users-table `overflow-x:auto` containment works as designed, and row-action buttons stay a clickable 32px tall. Found one real bug the static-code audit couldn't have caught: `dashboard.html`'s and `admin/users.html`'s inline edit-row forms live in a `<td colspan="8|5">` spanning the table's full (often wide, unscrolled) content width rather than the visible viewport; Pico's default `button[type=submit]{width:100%}` rule makes the bare `<button type="submit" class="save-edit-btn">Save</button>` inherit that full width. On `admin/users.html` (narrower table) this just pushes Save awkwardly past the edge with its label still barely visible; on `dashboard.html` (8-column table with real destination URLs pushing it past 1300px) the Save button balloons to ~1277px and its centered "Save" label is not visible at scrollLeft=0, the far right, or a scripted middle scroll position (950px) — confirmed via screenshot showing a blank blue bar. The button is presumably still clickable somewhere, but a real mobile user has no visible affordance to submit an edit. *(Resolved by the critique fix pass below — wrapped Save/Cancel in `role="group"`, exactly the fix anticipated here.)*

## Impeccable critique with browser automation + fix pass

- [x] `/impeccable critique gui/` — first critique run with Playwright available (dual-agent: independent design review + detector/browser-evidence pass) — done when: scored 25/40 (Acceptable), consistent with the project's historical 20-26/40 band. Zero actionable first-party findings from the static `detect.mjs` scan (2 hits, both known false positives). The live browser-injected detector found real issues the source-only critique rounds missed: `cramped-padding` on the slug-chip badge, `nested-cards`/`edge-flush-cards` on the row-action button group (corroborating a P1 below), and a flat type hierarchy (mostly an accepted DESIGN.md tradeoff, not a defect). New findings from the design-review pass: no `:focus-visible` treatment anywhere on the persistent nav; row-action buttons require horizontal scrolling to reach Delete even at 1440×900 (not just narrow viewports); `admin/users.html`'s Status column never got the `.status-badge` component `dashboard.html` already has; the edit-row's password `<small>` hint is ordered before its `<input>`, sidestepping Pico's stacking rule and causing a visual overlap into the neighboring grid column (this last one — a P2 — was scoped out of the fix pass below, still open). Snapshot: `.impeccable/critique/2026-07-24T04-28-44Z__gui.md`.
- [x] Fix pass on the critique's top 3 P1s, folding in the still-open narrow-viewport Save-button bug from the row above — file(s): gui/dashboard.html, gui/admin/users.html, gui/theme.css — done when:
  - **Row-action desktop overflow + narrow-viewport Save-button balloon** (`/impeccable layout`): added `.destination-cell` (max-width + ellipsis + `title` tooltip, full text still in the DOM for screen readers) to bound the one column with no natural length limit — cut a real long-URL test row's overflow from ~600px (Delete completely off-screen, confirmed at 1440×900) to ~20px (Delete fully visible, editorially reasonable residual from the slug chip showing a full origin+slug). Wrapped both `dashboard.html`'s and `admin/users.html`'s edit-row Save/Cancel in `<div role="group">`, matching Pico's own documented grouped-button pattern (`width:auto` + `flex:1 1 auto` children) instead of letting the bare `<button type="submit">` inherit `width:100%` from its wide `<td colspan>` — verified live: Save went from ~1277px-wide-with-invisible-label to a normal ~47px button, fully visible and reachable at both 1440×900 and 375px, on both pages.
  - **Missing Status Badge on `admin/users.html`** (`/impeccable extract`): migrated the existing `.status-badge`/`.status-active`/`.status-disabled` classes (already defined in `theme.css`, already used by `dashboard.html`) onto the users table's Status column — no new CSS needed, pure reuse of the already-extracted component. Verified live: renders bold `--ss-ok-500` green "Active", matching the links table exactly.
  - **No nav focus indicator** (`/impeccable harden`): added a `:focus-visible` outline (`--ss-signal-500`, 2px, full opacity) to `#app-header nav a`/`button` — Pico's default focus box-shadow was nearly imperceptible against the dark-navy nav fill. The project's design-detector hook caught one real issue in this step (an undocumented `0.15rem` radius) — fixed by reusing the existing `--pico-border-radius` token instead. Verified live: a clearly visible blue outline on keyboard focus.
  - Verified end-to-end (not just visually): Edit→Cancel collapses the row, Edit→change destination→Save persists via a real API round-trip and re-renders correctly; zero console errors throughout. `detect.mjs --json gui/` shows no new findings (same 2 pre-existing false positives). `uv run pytest` (118 tests) and `go test ./linkgate/...` both still pass (GUI-only change).
  - Deliberately out of scope (user chose "top 3 P1s only"): the password-input/`<small>`-ordering P2 bug and the slug-chip cramped-padding P2 remain open.
- [x] Re-critique after the fix pass — done when: independently re-verified all 4 fixes live via a fresh dual-agent critique (not just re-reading the diff) — all 4 landed cleanly with no regressions of their own, but re-verifying the Save/Cancel area surfaced a real, previously-unreported bug on the same surface: `#links-table td:last-child { white-space: nowrap; }` (written only for the row-actions cell) also matches the edit-row's own `<td colspan="8">` (also a last-child), forcing `nowrap` through to the Starts/Expires date grid and visually overlapping the "Expires" label into the Starts input — confirmed independently by both assessments and by a screenshot. Score: 26/40 (up from 25/40). Snapshot: `.impeccable/critique/2026-07-24T15-14-24Z__gui.md`.
- [x] Scoped fix pass on the re-critique's bugs (chose "bugs only," not the gaps/polish items) — file(s): gui/dashboard.html, gui/theme.css, DESIGN.md, .impeccable/design.json — done when:
  - **Edit-row Starts/Expires overlap** (P0): scoped the `:last-child` selector to `#links-table tbody > tr:not(.edit-row) td:last-child` so it no longer reaches into the edit-row's colspan cell — verified live at both 1440×900 (side-by-side, no overlap) and 375px (correctly stacked via Pico's grid collapse, no overlap).
  - **Password `<small>`/`<input>` ordering overlap** (P2, carried over from the prior critique): moved the `<small>` hint after its `<input>` in the "New link password" label, matching Pico's `:where(input,...)+small{display:block}` stacking rule used everywhere else in the app — verified live, label/input/hint now stack cleanly.
  - **Row actions still frequently unreachable** (P1): user chose a styled-scrollbar fix over a shadow/gradient edge-fade, since DESIGN.md's own No-Shadow Rule rules out synthetic depth cues — added `scrollbar-width`/`scrollbar-color` plus `::-webkit-scrollbar` styling (thin, Signal Blue thumb on a `border-mist` track) to `main figure`, replacing the OS default (esp. macOS's auto-hiding overlay scrollbar, which gave zero persistent hint that a table scrolls) with an always-visible, on-brand one. Verified applied via computed style (`scrollbar-width: thin`, `scrollbar-color` resolving to Signal Blue + border-mist).
  - **Bonus, user-approved housekeeping**: unified the two documented "error reds" — `.form-error` now reads `--ss-danger-500` instead of Pico's inherited `--pico-del-color`, matching status badges exactly (verified live: a triggered form error renders `rgb(179, 38, 30)` = `#b3261e`). Updated DESIGN.md's two prose references (the Inputs/Fields "Error:" line and the former "Don't introduce a third..." bullet) to describe the resolved state instead of the open inconsistency, and added a Layout-section line documenting the new scrollbar convention. Refreshed `.impeccable/design.json` (sidecar-only, per its own contract, since DESIGN.md was already hand-edited) — updated `generatedAt`, the `dos`/`donts` narrative arrays, the Persistent Nav Shell component's CSS (now includes the `:focus-visible` outline from the prior fix pass, previously never synced to the sidecar), and added a new "Scrollable Data Table" component entry.
  - Verified end-to-end: mechanical scan unchanged (same 2 known false positives, no new findings); `uv run pytest` (118 tests) and `go test ./linkgate/...` both still pass; zero console errors throughout.
  - Deliberately left open (user chose "bugs only" this round): password protection missing from the create-form UI, `links/detail.html` having no Edit/Copy entry point, the slug-chip cramped-padding P2, and the recent-events raw-timestamp P2.
- [x] Add password protection to the create-link form (the P1 gap left open above) — file(s): gui/dashboard.html — done when: added a "Password protection (optional)" field to the create-form (its own labeled row after the custom-slug/starts/expires grid, `minlength="4"` matching the backend's `MIN_LINK_PASSWORD_LENGTH`, placeholder "Leave blank for no password" since it's genuinely optional) — wired into the existing `POST /links` payload (only included when non-empty, matching the API's already-optional `password` field), reset alongside the other fields on success, and given the same `invalid_password` friendly-error override ("Link passwords must be at least 4 characters") already used by the edit-row's password flow, since the global message is worded for the 8-char user-account minimum. Verified as a full, real end-to-end round trip via Playwright, not just UI: created a link with a password, confirmed the dashboard row shows the `.lock-badge`, then hit the real `/r/{slug}` redirect and confirmed the redirect component's password gate actually rejects the wrong password (401, "Incorrect password") and accepts the right one (redirects to the real destination) — the same server-side gate every other password-protected link goes through, not a new code path. Also confirmed native `minlength` validation blocks a too-short password client-side before any API call fires. `detect.mjs --json gui/` unchanged (same 2 known false positives). `uv run pytest` (118 tests) and `go test ./linkgate/...` both still pass.
- [x] Give `links/detail.html` an Edit/Copy entry point (the other gap left open above) — file(s): gui/app.js, gui/dashboard.html, gui/links/detail.html — done when: added a Copy button and an Edit button next to the detail page's slug-chip `h1`, wrapped in a `<div role="group">` matching the row-actions convention. Promoted the previously dashboard.html-local `copyToClipboard()` helper to `app.js` so it's shared instead of duplicated (dashboard row Copy, success-banner Copy, and this new detail-page Copy all now call the one implementation). Edit doesn't re-implement the edit form a second time — it deep-links to `dashboard.html?edit=<slug>`, and a small `openDeepLinkedEditRow()` added to `dashboard.html`'s init reads that query param, clicks the matching row's existing Edit button, and scrolls it into view — so the round trip the critique flagged becomes one click landing directly on the right, already-expanded edit row, without duplicating any CRUD logic across two pages. Caught and fixed one regression of my own during verification: the new `role="group"` wrapper hit the exact same Pico `width:100%`/`flex:1 1 auto` bug already fixed elsewhere this session, stretching Copy/Edit across the full container width — fixed with the same `[role=group] { width: auto; }` scoping pattern, this time scoped to `.detail-heading [role=group]`. Verified live: Copy actually writes to the clipboard (button flips to "Copied!"), Edit's `href` resolves correctly, clicking it lands on the dashboard with the right row's edit form already expanded and scrolled into view, and the heading area renders correctly (no width regression) at both desktop and 375px. `detect.mjs --json gui/` unchanged (same 2 known false positives — the `#qr-preview` flag re-confirmed as a false positive on every edit to this file this session). `uv run pytest` (118 tests) and `go test ./linkgate/...` both still pass.
- [x] Fix the last two P2 polish items from the critique — file(s): gui/theme.css, gui/links/detail.html — done when:
  - **Slug-chip cramped padding**: root-caused to `.slug-chip`'s vertical padding being em-relative (`0.15em`) — fine at the detail page's larger `h1` font size, but only ~1.7px at the dashboard table's smaller font size (the exact context the detector measured). Switched to a fixed `0.3rem` vertical padding (horizontal stays `0.6em`) so the chip keeps an adequate, context-independent legibility floor everywhere it renders. Verified live: computed padding at the dashboard table's 11.56px font-size context is now 4.8px (was ~1.7px), clearing the ~4.0px floor; the detail-page `h1` context still looks proportionate, no visual regression.
  - **Recent-events raw timestamp**: `links/detail.html`'s events table rendered `event.timestamp` as a raw ISO8601 string instead of through the page's own `formatDateTime()`, the one place in the whole app that skipped it. One-line fix. Verified live by generating a real click (`/r/<slug>`) and confirming the "When" column now renders `Jul 24, 2026, 12:19 PM` instead of `2026-07-24T...Z`.
  - Verified end-to-end: `detect.mjs --json gui/` unchanged (same 2 known false positives). `uv run pytest` (118 tests) and `go test ./linkgate/...` both still pass. This closes out every item raised by the 6th critique pass — nothing left open from that snapshot.

## Client-side window validation + styled confirmation dialogs

- [x] Client-side start_at/end_at validation on both link forms — raised and implemented 2026-07-24, direction confirmed with the user first (native validation bubble, not a `.form-error` message) — file(s): gui/dashboard.html — done when: added `wireWindowValidation(startInput, endInput)`, a shared local helper wired to both the create-form's `#start-at`/`#end-at` and each row's dynamically-created `.edit-start-at`/`.edit-end-at` pair. Mirrors the server's own `invalid_window_range` condition exactly (start >= end is invalid, not just start > end) via `setCustomValidity()` on the end field, re-evaluated on every `input` event on either field so a since-fixed combination clears the message instead of staying stuck invalid. `datetime-local` values are fixed-width `YYYY-MM-DDTHH:mm` strings, so a plain string comparison is already correct chronological ordering — no `Date` parsing needed. Verified live: setting Expires before Starts blocks submission natively (confirmed zero API calls fired and no link/edit was created) with the message "Expires must be after Starts."; fixing the dates clears the message and the submit goes through.
- [x] Replace native `confirm()` with a styled, on-brand confirmation for link/user delete — raised and implemented 2026-07-24, four design decisions confirmed with the user first (native bubble vs. inline error was the window-validation question above; this one covered dialog style, shared-vs-duplicated implementation, and confirming scope was just the two existing `confirm()` call sites) — file(s): gui/app.js, gui/dashboard.html, gui/admin/users.html, DESIGN.md, .impeccable/design.json — done when: added a shared `confirmDialog(message, {confirmLabel, cancelLabel})` helper to `app.js` (Pico's native `<dialog>`/`<article>` component, built dynamically so no markup is duplicated per page; resolves a promise; dismissible via its own Cancel button, the Esc key — which fires `cancel`, not `close`, on a native dialog — or a backdrop click, detected as a click landing on the `<dialog>` element itself rather than its `<article>` content). Both `dashboard.html`'s link-delete handler and `admin/users.html`'s user-delete handler now `await confirmDialog(...)` instead of calling `confirm(...)`. Button styling deliberately inverts the row-level convention: Cancel renders as Pico's plain default (primary) button while Confirm/Delete stays `outline secondary` — the same de-emphasis the row-level Delete button already uses (per DESIGN.md's existing "de-emphasis, not warning color, is how destructive reads here" rule) — so the visually prominent button in the dialog is the safe one, not the destructive one. Documented as a new "Confirmation Dialogs" component in `DESIGN.md` and added a matching entry + `generatedAt` refresh to `.impeccable/design.json`. Verified live for both delete flows: the dialog renders with the correct message and on-brand styling; Cancel closes without deleting; Esc closes without deleting; Confirm actually deletes via the real API and the row disappears from the table.
- Verified end-to-end: `detect.mjs --json gui/` unchanged (same 2 known false positives). `uv run pytest` (131 tests, unchanged — GUI-only change, no backend touched) and `go test ./linkgate/...` both still pass.
- [x] Fix asymmetric confirm-dialog layout (user-reported: "weird line breaks", not more symmetrical) — raised and fixed 2026-07-24 — file(s): gui/app.js, gui/theme.css, DESIGN.md, .impeccable/design.json — done when: root-caused to Pico's own `dialog>article>footer{text-align:right}` default combined with a left-aligned message and no width cap — for a short one-line Yes/No message inside Pico's wide (~700px, empirically measured) default article, this produced a lopsided box: text hugging the left edge, buttons hugging the right, with a large empty gutter on both sides. Added a `.confirm-dialog` class (applied by `confirmDialog()`) capping the article at `26rem` and centering both the message and the footer button row. Verified live at both a short one-line message and a long custom-slug message that actually wraps to two lines — both render as a compact, centered, symmetrical box now. Updated `DESIGN.md`'s Confirmation Dialogs entry and refreshed the `.impeccable/design.json` sidecar's component snippet to match. `detect.mjs --json gui/` unchanged (same 2 known false positives); `uv run pytest` (131, unchanged) and `go test ./linkgate/...` both still pass.

## Impeccable audit (first since the very first 15→19/20 pass) + P0 fix

- [x] `/impeccable audit gui/` — done when: scored 11/20 (Acceptable), pulled down almost entirely by one severe, previously-undetected finding. While spot-checking the new confirm dialog's button color via `getComputedStyle`, found that `--pico-primary-background` resolved to Pico's raw default `#0172ad`, not the theme's `#276fb8` — traced this to a real CSS specificity bug, not a fluke: `theme.css`'s `:root { --pico-primary-background: #276fb8; ... }` (specificity 0,1,0,0) has always lost the cascade against `vendor/pico.min.css`'s own `:root:not([data-theme="dark"]) { --pico-primary-background: #0172ad; ... }` (specificity 0,2,0,0) — higher specificity wins regardless of load order. Confirmed via `git log -p` that this has been broken since `theme.css`'s very first commit (`e93ac50`), surviving 2 full critique rounds and 1 prior audit undetected, because the coincidental fallback values (`#0172ad` on white still clears ~5.24:1 AA) never triggered a visible failure. Systematically diffed every property in both `:root` blocks via the live CSSOM (not just the one primary-color instance) and found **19 of 30 intended overrides silently dead**: the full `--pico-primary*` family (10 properties), `--pico-background-color`, `--pico-color`, `--pico-h1/h2/h3-color`, `--pico-muted-color`/`-border-color`, and `--pico-card-*-color` (3 properties) — meaning page background, body text, headings, and every primary button have been rendering Pico's stock light-theme colors instead of the documented dark-navy identity this entire time. The `--ss-*`-prefixed tokens (nav, slug chip, status badges) were never affected since Pico has no competing declaration for those names, which is exactly why the nav/chips/badges have always looked correct while the rest silently didn't. Also found 2 real accessibility gaps (confirm dialog has no `aria-labelledby`; row-action buttons still lack `aria-label` disambiguation, carried over from the earlier critique) and 1 forward-looking performance note (full DOM rebuild on every sort/filter — fine at current scale). Responsive and the mechanical scan (`detect.mjs`) came back clean (same 2 known false positives).
- [x] Fix the P0 theming/cascade bug — file(s): gui/theme.css, DESIGN.md — done when: changed `theme.css`'s selector from plain `:root` to `:root:not([data-theme="dark"])`, exactly matching Pico's own selector so specificity ties and `theme.css`'s later load order in every page's `<head>` correctly breaks the tie in the theme's favor — zero HTML changes needed. Verified live via `getComputedStyle`: all 19 previously-broken properties now resolve to their intended values (`--pico-primary-background: #276fb8`, `--pico-background-color: #eef1f6`, `--pico-color: #14243d`, `--pico-h1-color: #0a1628`, etc.), confirmed on login, dashboard, admin/users, and the confirm dialog — a genuinely visible difference (darker headings, a real mist-tinted page background instead of pure white) in side-by-side screenshots, not just a numeric confirmation. Added a `Don't declare theme color-variable overrides at plain :root` entry to `DESIGN.md` so this specific mistake doesn't recur if a future override is added the same way. `detect.mjs --json gui/` unchanged (same 2 known false positives); `uv run pytest` (131) and `go test ./linkgate/...` both still pass.
- [x] Fix the audit's 2 P2 accessibility findings — file(s): gui/app.js, gui/dashboard.html, gui/admin/users.html — done when:
  - **Confirm dialog has no accessible name**: `confirmDialog()` now generates a unique `id` for the message `<p>` (a module-level counter, since the dialog is created fresh per call) and sets `aria-labelledby` on the `<dialog>` pointing to it. Verified live: `document.getElementById(dialog.getAttribute('aria-labelledby')).textContent` correctly returns the actual message text ("Delete the link...") instead of the dialog having no computed accessible name.
  - **Row-action buttons lack disambiguation**: added `aria-label` to every row action on both pages — `dashboard.html`'s View/Copy/Edit/Delete (`"View link {slug}"` etc.) and `admin/users.html`'s Edit/Delete (`"Edit user {username}"` etc.) — preserving the visible short label while giving each control a unique accessible name. Verified live via the accessibility tree: buttons now read as `"View link aria-check"`, `"Delete user admin"`, etc. instead of generic, indistinguishable "Delete, button" entries across multiple rows.
  - Verified end-to-end: `detect.mjs --json gui/` unchanged (same 2 known false positives); `uv run pytest` (131) and `go test ./linkgate/...` both still pass.
- [x] Fix the audit's P3 performance note — file(s): gui/dashboard.html — done when: the actual anti-pattern wasn't the `<tr>` markup rebuild itself (genuinely cheap at this app's realistic scale) but `renderLinksTable()` re-querying every row's Copy/Edit/Delete/Cancel button and the edit-form via `body.querySelectorAll(...).forEach(el => el.addEventListener(...))` and attaching a fresh listener set on *every* render — which fires on every sort click and every (debounced) filter keystroke, so the re-listening cost scaled with row count for zero benefit, since the buttons' behavior never actually depends on which render produced them. Replaced with two delegated listeners on `#links-body` (one `click`, one `submit`), attached once at page init, dispatching to named handler functions (`handleCopyClick`/`handleDeleteClick`/`handleEditToggleClick`/`handleCancelEditClick`/`handleEditFormSubmit`) via `event.target.closest(...)` — `renderLinksTable()` itself now only builds row markup, no listener wiring. `submit` bubbles by default (unlike some other form events), so the delegated submit listener needed no special-casing. Verified live, not just by reading the diff: created 2 links, sorted (the exact path that used to re-attach every listener), then exercised Copy, Edit-toggle, Cancel, a full Edit→Save round-trip (confirmed the API call and the visible row update), Delete-with-confirm-dialog, and the filter box — all worked correctly through the new delegated handlers, zero console errors across the whole sequence. `detect.mjs --json gui/` unchanged (same 2 known false positives); `uv run pytest` (131, unchanged — GUI-only) and `go test ./linkgate/...` both still pass.

## Impeccable critique (8th pass, 27/40 — first drop, from genuinely new findings) + 3 P1s + 2 P2s fixed

- [x] `/impeccable critique gui/` (8th pass) — done when: scored 27/40 (Acceptable), the first drop in this project's critique trend (20→26→23→26→25→26→30→27), driven entirely by fresh, previously-undetected findings rather than any regression in the prior pass's fixes (self-delete/admin-promotion confirmations, sticky action column, user-creation payoff, and progressive disclosure all re-verified working correctly). Headline finding, confirmed independently by both assessments and by source inspection: every `<article>` card on every page has been silently rendering Pico's default drop shadow since `theme.css`'s first commit (`e93ac50`), directly violating DESIGN.md's own "No-Shadow Rule" — `theme.css` overrides every other `--pico-card-*` property but never touched `--pico-card-box-shadow`, which defaults to `var(--pico-box-shadow)`, a real 7-layer shadow. Also found: the links table overflows its own container by 21px at a realistic 1400px width with only 4 seeded rows, clipping the "Expires" header; at 390px, the just-shipped sticky action column occludes other columns' content at rest (not merely requiring a scroll) — confirmed pixel-precise (73.6px full overlap of the Owner column on the links table); Edit on an off-screen row gives no scroll-into-view; and deleting a link leaves a stale "Link created" success banner with a live Copy button pointing at the now-dead link. Full report: `.impeccable/critique/2026-07-25T00-09-22Z__gui.md`.
- [x] Fix the 3 P1s + 2 P2s (user chose "Everything") — file(s): gui/theme.css, gui/dashboard.html, gui/admin/users.html, DESIGN.md, .impeccable/design.json — done when:
  - **No-Shadow Rule violation**: added `--pico-card-box-shadow: none;` and `--pico-dropdown-box-shadow: none;` to `theme.css`'s `:root:not([data-theme="dark"])` block. Verified live via `getComputedStyle` on the login card: `boxShadow` now resolves to `"none"` (previously a real multi-layer shadow).
  - **Desktop table overflow at 1400px**: the links table's slug chip dropped the redundant origin prefix (identical on every row, e.g. `http://host`) — table cells now show `/r/{slug}` with the full URL in a `title` attribute; Copy/View still use the full URL internally. Verified live: `table.scrollWidth` now exactly equals `figure.clientWidth` (1176 = 1176, was 1197 vs 1176) with the same realistic seeded data (including a long UTM-tagged destination URL) that previously overflowed.
  - **Mobile sticky-column occlusion (390px)**: added a `@media (max-width: 600px)` block to both tables. Links table hides Owner/Destination/Created/Starts, keeping Short link/Status/Expires/Actions (4 columns, aligning with the cognitive-load "≤4 items" guidance); users table hides Permissions. This alone wasn't sufficient — the sticky action column's own rendered width (208px for 4 unwrapped buttons) still didn't leave room for the kept columns, so the action button group also gets `flex-wrap: wrap; max-width: 100px` (70px on the users table's 2-button group) to shrink its footprint. Even after both fixes, a small residual overlap remained on whichever column sits immediately before the sticky column (confirmed live via `getBoundingClientRect`, e.g. "expiring soon" text and "active"/"Status" text partially covered). **Two attempted fixes measured to have zero real effect and were reverted**: `padding-right` alone (adds space after left-aligned content without moving it) and `padding-right` + `text-align: right` (the padding terms algebraically cancel once the cell's own width is also padding-driven in an auto-layout table) — both confirmed via direct pixel measurement to not move the text's rendered position at all across different padding values. The fix that actually works: an explicit `max-width` on the column itself (75px for links' Expires, 60px for users' Status), forcing wrapped text to physically stay within a zone that can never reach the sticky column's fixed screen position. Verified live via `getBoundingClientRect` comparison (not just screenshots): both columns now report `clear: true` (content's rightmost extent ≤ the sticky column's left edge) with real seeded content (`"Jul 25, 2026, 8:00 AM (expiring soon)"`, a long comma-joined permissions-driven "active" status) — confirmed via before/after screenshots showing full, unclipped, cleanly-wrapped text instead of "expiri[ng soon)]" / "Statu[s]" / "Activ[e]" mid-word clipping.
  - **Edit scroll-into-view**: `handleEditToggleClick` (dashboard.html) and the `.edit-btn` handler (admin/users.html) now call `editRow.scrollIntoView({ block: "center" })` when opening (not closing) a row. Verified live at a short viewport (500px): a row measured at `top: 711` (off-screen) scrolled its revealed edit form to `top: 135` (centered in view) after the click.
  - **Stale success banner after delete**: `handleDeleteClick` (dashboard.html) and the users-table delete handler (admin/users.html) now hide `#create-success` on a successful delete. Verified live: created a link, confirmed its success banner + Copy button appeared, deleted that same link, confirmed the banner was hidden immediately afterward (previously it stayed visible with a Copy button pointing at a dead slug).
  - Updated `DESIGN.md` (Elevation & Depth section corrected to document the shadow fix rather than the pre-existing incorrect claim that the theme "never overrides" card box-shadow; Data Tables section documents the mobile column-collapse technique and explicitly warns that `padding-right` does not create sticky-column clearance; Slug Chip component note documents the table's shortened-chip convention) and refreshed `.impeccable/design.json` (Scrollable Data Table and Slug Chip component descriptions extended; 2 new `dos` entries).
  - Regression-verified: full desktop re-check (table no longer overflows at 1400px), sort, Copy, Edit-toggle/Cancel, and a full Delete-with-confirm-dialog round-trip all still work correctly through every CSS change, zero console errors across the entire verification session (only a pre-existing, unrelated `autocomplete`-attribute advisory, not from any code in this repo).
  - `detect.mjs --json gui/` unchanged (same 2 known false positives). `uv run pytest` (131, unchanged — GUI-only change) and `go test ./linkgate/...` both still pass.

## Impeccable audit (2nd since the theming/shadow fixes, 12/20) + P0 + 2 P1s fixed

- [x] `/impeccable audit gui/` — done when: scored 12/20 (Acceptable), the first audit since the box-shadow fix, the mobile column-collapse work, and the sticky-column changes from the last two critique passes. Confirmed genuinely fixed and still holding: the No-Shadow Rule (`getComputedStyle(article).boxShadow === "none"` on every card, every page) and the color-cascade specificity fix (all intended navy/blue values still resolve correctly). Detector clean (2 known false positives only). Found 1 new P0 and several P1/P2/P3s. **P0**: every text input sitewide (starting with the login screen) silently renders Pico's stock dark-mode fill (`rgb(28,33,44)` background, light text) on any OS/browser set to prefer dark colors — `theme.css` overrides every other Pico color-variable family but never `--pico-form-element-*`, and no `data-theme` attribute is ever set anywhere in the app, so there's no opt-out. **P1**: the "Active" status badge measures 4.39:1 contrast against the table cell's real background (`--pico-background-color`, since Pico paints `td`/`th` with that token, not the white card behind it) — just under the 4.5:1 AA minimum for the single most common status label in the app. **P1**: the default focus ring measures ~1.71:1 (needs 3:1) on every focusable element except the persistent nav, which had already been fixed for this exact problem in an earlier pass but never generalized — including the newly-added "More options" disclosure toggle. Also found (not yet fixed, user scoped to P0+2 P1s only): the dashboard's mobile Expires-column `max-width` cap only applied to `<td>` not `<th>` (unlike the equivalent, correctly-built fix on the users table), zero-gap stacked mobile action buttons, and a custom slug longer than ~10-12 characters visibly deforming the slug-chip's pill shape.
- [x] Fix the P0 + 2 P1s (user chose "P0 + 3 P1s", scoped in practice to these 3 — dark-mode leak, status contrast, focus-ring contrast) — file(s): gui/theme.css, DESIGN.md, .impeccable/design.json — done when:
  - **Dark-mode form-input leak**: added `color-scheme: light;` plus explicit `--pico-form-element-background-color`/`-border-color`/`-color`/`-placeholder-color` overrides to `theme.css`'s existing `:root:not([data-theme="dark"])` block. Verified live via Playwright's `page.emulateMedia({colorScheme: 'dark'})`: the login page's Username input still resolves to `rgb(255,255,255)` background / navy text (previously would have shown Pico's dark fill).
  - **Status badge contrast**: darkened `--ss-ok-500` from `#1a7f5a` to `#177251` — same fix pattern already used for `--ss-signal-500`. Verified live: computed contrast of the "Active" badge against its real table-cell background is now 5.21:1 (was 4.39:1).
  - **Focus ring contrast**: replaced the nav-scoped `#app-header nav a:focus-visible, #app-header nav button:focus-visible` rule with a sitewide `:focus-visible { outline: 2px solid var(--ss-signal-500); ... }`. **Found and fixed a second-order issue during verification, not in the original plan**: Pico defines its own higher-specificity `details summary:focus-visible:not([role])` rule, which silently won over the new generic rule for the create-link form's "More options" toggle specifically (confirmed live: the toggle still showed the old low-opacity outline after the generic fix). Added a matching-selector-shape override (`details summary:focus-visible:not([role])`) so specificity ties and load order breaks it in this file's favor — the same trick already used for the `:root` color-cascade fix. Verified live via `page.locator(...).focus()` + `getComputedStyle`: both a plain row-action button and the "More options" summary now show a full-opacity `rgb(39,111,184)` 2px solid outline.
  - Updated `DESIGN.md` (Status Badges' color value updated with the contrast rationale; new "Focus Indicators" component section documenting the sitewide rule and the `<summary>`-specific specificity gotcha; Inputs/Fields section documents the `color-scheme: light` fix) and `.impeccable/design.json` (Status Badge component + `ok-green` colorMeta/tonal-ramp updated; new Focus Indicators component; 2 new `dos` entries).
  - Regression-verified: full create→copy→edit→cancel→delete round-trip still works, zero console errors.
  - `detect.mjs --json gui/` unchanged (same 2 known false positives). `uv run pytest` (131, unchanged — GUI-only) and `go test ./linkgate/...` both still pass.

## Fix the audit's 3 remaining P2s (leftover from the last audit's "P0 + 3 P1s" scope)

- [x] Fix dashboard's incomplete `th`/`td` column cap, add gaps between stacked mobile action buttons, and fix the slug-chip pill wrapping on long custom slugs — raised 2026-07-25 — file(s): gui/dashboard.html, gui/admin/users.html, DESIGN.md, .impeccable/design.json — done when:
  - **Dashboard's Expires column cap**: added the missing `#links-table thead th:nth-child(7)` rule alongside the existing `td:nth-child(7)` one (an auto-layout table sizes a column to its widest cell across every row, header included — capping only `td` left the header's own width in control). Verified live: both `th` and `td` now report exactly 75px (was 80.4px).
  - **Zero-gap stacked mobile buttons**: added `gap: 0.25rem` to both tables' wrapped `[role=group]` rules. **Found and fixed a deeper bug during verification, not in the original plan**: `admin/users.html`'s plain Edit/Delete buttons were never wrapped in a `<div role="group">` at all (only the edit-row's Save/Cancel were) — so the `gap`/`flex-wrap`/`max-width` rule targeting `[role=group]` had silently matched nothing there the whole time; the buttons were stacking via ordinary inline-block wrapping with a real, confirmed **zero-pixel** gap. Added the missing wrapper in the JS row template, matching the convention already used everywhere else in the app, plus the same `width: auto` override every other `[role=group]` here needs (Pico's default `width: 100%` would otherwise balloon it to the sticky column's full width). Verified live: dashboard.html's stacked buttons now show a real 4px gap; users.html's now show a real 4px gap too (previously exactly 0px, confirmed by the new button's bottom edge exactly matching the next one's top edge).
  - **A second, more severe bug found and fixed during verification of the column caps**: a single long custom slug *anywhere* in the links table (not necessarily the row being checked) widens the shared Short-link column for every row, which pushes the Expires column's *position* rightward and silently defeats its clearance — even though Expires' own `max-width` cap never changed. Confirmed live: a table containing both a short-slug row and the `black-friday-2026` custom-slug row broke a clearance that measured correctly in isolation with only short slugs present. Fixed by adding a matching `max-width: 100px` cap to the Short-link column (`th`/`td:nth-child(1)`) itself. Re-verified with both a short-slug row and the long custom-slug row present simultaneously in the same table: `clear: true` on both.
  - **Slug chip pill wrapping**: added `white-space: nowrap; overflow: hidden; text-overflow: ellipsis; max-width: 12rem` scoped to `#links-table .slug-chip` only (never the shared `.slug-chip` class itself, which the link-detail page's `h1` also uses and must keep showing the complete URL). In practice, the seeded `black-friday-2026` slug renders at 153px — under the 192px cap — and doesn't actually need to truncate now that the chip already dropped the redundant origin prefix (see the prior desktop-overflow fix); the cap is defensive for any slug up to the full 32-char maximum, not a fix for a currently-reproducing wrap. Verified live: the table chip renders on one line; the detail page's `h1` for the same link still shows the full untruncated `http://host/r/black-friday-2026`.
  - Updated `DESIGN.md` (3 new notes under Layout/Data-tables documenting the header-cap gotcha, the shared-column-width cascade gotcha, and the `[role=group]`-wrapper requirement; Slug Chip note extended) and `.impeccable/design.json` (Scrollable Data Table and Slug Chip descriptions extended; 2 new `dos` entries).
  - Regression-verified: full create→edit→cancel→sort→delete on dashboard.html, and create→edit→self-delete-hide→admin-promotion-confirm→delete on admin/users.html, all still work correctly; desktop (1400px) users.html shows Edit/Delete normally side-by-side, not ballooned. Zero console errors throughout.
  - `detect.mjs --json gui/` unchanged (same 2 known false positives). `uv run pytest` (131, unchanged — GUI-only) and `go test ./linkgate/...` both still pass.

## Impeccable audit (3rd pass, 14/20 — re-audit after the last fix pass) + P0 + P2 fixed

- [x] `/impeccable audit gui/` (3rd pass) — done when: scored 14/20 (Good, up from 12/20), with every item from the 2nd audit pass independently re-verified live and confirmed genuinely holding (status badge 5.21:1 contrast, focus ring full-opacity everywhere including the "More options" `<summary>` edge case, both mobile column-clearance fixes including the long-slug-elsewhere-in-the-table scenario, the 4px button gaps, the side-by-side desktop layout, the slug-chip table/detail-page scoping) — none of the prior fixes had regressed. Found 1 new P0 and 1 new P2. **P0**: the dark-mode form-input fix from the 2nd audit pass turned out to cover only the *resting* state — Pico defines a separate `--pico-form-element-active-background-color` variable that only applies on `:active`/`:focus`, with its own distinct dark-mode value never added to `theme.css`'s override block, so the instant any input received focus under a dark-mode OS/browser preference, its background fell back to Pico's dark fill (measured ~1.06:1 contrast — functionally invisible text). **P2**: every button in the app measures 40px tall, under the 44px touch-target guideline, on a system with real mobile-specific layout. Also flagged as a P3 (not fixed, low priority): `.slug-kind-badge`/`.lock-badge` text clears AA contrast with only 0.27:1 margin — not broken, just fragile to a future palette tweak.
- [x] Fix the P0 + P2 (user chose "both") — file(s): gui/theme.css, DESIGN.md, .impeccable/design.json — done when:
  - **Dark-mode focused-input leak**: rather than patching only the one reported variable, grepped `vendor/pico.min.css` for the entire `--pico-form-element-*` family and cross-referenced every variable's light vs. dark-mode value to find every sibling with a genuine divergence not already covered indirectly through an overridden `--pico-primary*`/`--ss-*` token. Found and fixed 4: `--pico-form-element-active-background-color` (the confirmed bug), `--pico-form-element-invalid-border-color`, `--pico-form-element-invalid-active-border-color`, and `--pico-form-element-selected-background-color`. **Also corrected a factual error in the existing code comment**: an earlier fix pass's comment claimed `color-scheme: light` was "the actual fix" for the dark-mode leak — it isn't; that property only hints the browser's native chrome (scrollbars, autofill) toward light rendering and has no effect on which CSS rules match `prefers-color-scheme`. The real mechanism (unchanged, just now correctly documented) is that every override is declared unconditionally, outside any media query, so it always wins. Verified live: with `page.emulateMedia({colorScheme: 'dark'})`, a focused input now stays `rgb(255,255,255)` background / navy text (previously fell back to Pico's dark fill).
  - **Touch target size**: added a sitewide `min-height: 44px` to `button, [type=submit], [type=button], [type=reset], [role=button]` — a height floor rather than loosening `--pico-form-element-spacing-vertical` (which also drives every text input's tap target and would have undone the app's deliberate admin-density type scale). Verified live: form-submit and row-action buttons now measure exactly 44px (was 40px), on both desktop and the mobile stacked-button groups; re-verified the mobile sticky-column clearance and button-gap fixes still hold with the taller buttons (they do — clearance is a horizontal measurement, unaffected by height).
  - Updated `DESIGN.md` (Buttons section documents the min-height floor and rationale; the form-element dark-mode note corrected and extended to document the 2-round fix history; new Do/Don't entries — sweep a whole Pico variable family when fixing one leak, and don't assume `color-scheme` blocks `prefers-color-scheme` matching) and `.impeccable/design.json` (Primary Button's stale, pre-fix focus-ring CSS snapshot corrected; Focus Indicators re-confirmed; 2 new dos/donts).
  - Regression-verified: full create→edit→delete round-trip still works, zero console errors.
  - `detect.mjs --json gui/` unchanged (same 2 known false positives). `uv run pytest` (131, unchanged — GUI-only) and `go test ./linkgate/...` both still pass.
- [x] Fix the P3 (slug-kind/lock-badge contrast margin) — file(s): gui/theme.css, DESIGN.md, .impeccable/design.json — done when: darkened `--ss-slate-500` from `#5b6b85` to `#526078` — it had cleared AA against the real table-cell background by only 0.27:1 (4.77:1), the same "measured against the wrong assumed background" class of mistake that caused the ok-green status-badge P1 fixed two audit passes ago. `--ss-slate-500` and Pico's own `--pico-muted-color` coincidentally shared the identical `#5b6b85` hex before this fix but are separate tokens; only `--ss-slate-500` (used by `.slug-kind-badge`/`.lock-badge`/`.not-yet-live`, confirmed via grep to have no other usages) changed — `--pico-muted-color` is untouched. Verified live: `.slug-kind-badge` (a seeded `black-friday-2026` custom slug's "Custom" label) and `.not-yet-live` (a seeded future-`start_at` link) both now measure 5.62:1 against their real `rgb(238,241,246)` cell background, up from 4.77:1. Updated `DESIGN.md` (Slate Muted color entry documents the two-tokens-one-coincidental-hex history) and `.impeccable/design.json` (new `badge-slate` colorMeta entry; Slug-Kind/Lock Badges component description and CSS snapshot updated). Zero console errors; `detect.mjs --json gui/` unchanged (same 2 known false positives); `uv run pytest` (131, unchanged — GUI-only) and `go test ./linkgate/...` both still pass.

## Considered and rejected

- [x] Client-side destination-URL reachability check on link creation — considered 2026-07-18, decided against, not deferred. The idea: before/without blocking link creation, check that a submitted `target_url` is actually reachable, run from the *browser* rather than server-side (since a real user's browser is far less likely to get bot-blocked by the destination site than a server making the same request). Rejected because neither `redirect` nor `api` has any `allowed_outbound_hosts` entries (confirmed empty/absent in `spin.toml` for both), so the server-side half of that reasoning is moot — the check can only ever run client-side. And a browser-side check against an arbitrary cross-origin URL is limited to a `fetch(..., {mode: "no-cors"})` opaque request, which can detect a hard DNS/connection failure but can never distinguish a real `200` from a `404`/`500` (no readable status across origins without CORS headers from the destination) — not enough signal to justify the added complexity, especially since the check could never be allowed to block submission anyway. Revisit only if a genuinely new mechanism becomes available (e.g. an outbound-allowed validation proxy), not as a plain client-side nudge.

## Future work (not scheduled)

All items below are complete-and-shipped-adjacent or deliberately deferred — not gaps found by accident — each has fuller rationale at the file(s) noted. Re-read those before starting any of them, since the decision not to do it yet may no longer apply.

- [ ] Set up CI (e.g. GitHub Actions) to run `go test ./linkgate/...` and `uv run pytest` automatically — explicitly deferred in Phase 3 ("local `go test`/`pytest` only; CI is a fast-follow, not in scope now"). No file(s) yet since nothing exists.
- [ ] Consolidate `redirect`/`api` from three named KV stores (`links`, `users`, `analytics`) to a single `"default"` store with key-prefixing (`links:`/`users:`/`analytics:`) — required before an actual Akamai Functions deploy, since Akamai only supports one auto-provisioned `"default"` store and no `runtime-config.toml`. See CLAUDE.md's "Deployment: known Akamai Functions blocker" section for the confirmed finding and exact prefix scheme. Touches `spin.toml`, `redirect/main.go`'s `kv.Open` calls, `api/app.py`'s `key_value.open` calls, and every key literal across both components' non-test and test code.
- [ ] Implement SAML and/or OIDC as additional auth providers — the seam is real and documented in `docs/auth-providers.md` (the `initiate`/`handle_callback` interface, `oauth_state` KV bookkeeping, JIT user provisioning), but neither provider is built. Needs a specific XML-signature (SAML) or JWT/JWKS (OIDC) validation library that works under componentize-py's pure-Python-only WASI constraint.
- [ ] Add security response headers (CSP, `X-Content-Type-Options`, `Referrer-Policy`, HSTS, etc.) — currently neither `redirect` nor `api` set any beyond `Content-Type`/`Cache-Control` where relevant. See CLAUDE.md's "Security tradeoffs" section — this one is a real gap worth closing, not a structural constraint like the others there.
- [ ] Multi-domain short-link hosting + admin-managed destination domain allow/deny-list — file(s): spin.toml, api/users.py, api/links.py, api/app.py, gui/dashboard.html, gui/admin/ (new domains-management page) — raised 2026-07-18, combining two related ideas: (1) the shortener currently has exactly one base domain, hardcoded via the single `public_base_url` Spin variable (used today only for QR-code encoding) — replace it with a KV-backed domain registry an admin can register/enable/disable entries in, supporting more than one domain; (2) separately, an admin-managed allow/deny-list constraining which domains a link's `target_url` is permitted or forbidden to point to (a content/abuse control, independent of which domain the short link itself lives under). Permission model decision already made: give each user a new `assigned_domains` list field (parallel to, but structurally independent of, the existing `permissions` list) rather than dynamically-generated per-domain permission strings — `auth.py`'s `KNOWN_PERMISSIONS` is deliberately a small, fixed, hardcoded vocabulary ("reject anything outside this set rather than silently accepting typos"), and extending it at runtime per registered domain would break that design. Each link record needs a new `domain` field (which domain it was published under, driving the displayed short URL and QR code). The create-link form should show a domain multi-select (checkboxes: one, several, or all of the user's assigned domains) only when the user has any assigned domains at all. One design question deliberately left open for whenever this is actually picked up, not resolved now: whether `redirect`'s hot path should validate the incoming `Host` header against a link's `domain` (an extra server-side check) or treat `domain` purely as display/QR metadata, since `/r/{slug}` resolution is currently Host-header-agnostic. This is a substantial, multi-file feature (permission model, link schema, QR generation, GUI create-flow, a new admin page) — re-confirm scope/priority before starting, don't assume this write-up is a complete spec.
- [ ] Bulk link management on the dashboard — raised 2026-07-24 (Alex/power-user persona finding from the Impeccable critique: "No bulk operations, no export... exactly the workload this persona represents" for someone managing dozens of campaign links one row at a time). Not scoped in detail yet — re-plan before starting. Rough shape: a per-row select checkbox (own column, first or last position) plus a "select all visible" control in the table header, and a bulk-action bar (Delete, Enable/Disable at minimum; CSV export is a separate, simpler nice-to-have that doesn't need the selection UI at all) that appears once ≥1 row is selected. Must respect the permission model built for `links.edit_all` below — a user should only be able to select/bulk-act on rows `canEditLink()` already says they can edit; selection state should probably clear on any change to the underlying link list (filter, sort, or a completed bulk action) rather than trying to persist selection across a re-render. Bulk delete in particular needs a real confirmation step (not N silent individual deletes) given how destructive it is at scale — worth deciding whether that's a styled modal/dialog or an extension of the existing native `confirm()` pattern used for single-row delete today.
- [x] Add a `links.edit_all` permission letting non-admin users edit/delete/password-protect other users' links — raised and implemented 2026-07-24. Plan confirmed with the user before writing (below), then built and verified end-to-end.
  - **Backend** — file(s): api/auth.py, api/links.py, api/analytics.py, api/qr.py — done when: added `"links.edit_all"` to `KNOWN_PERMISSIONS`; added two shared helpers in `links.py` — `can_view(principal, record)` (owner OR `links.view_all` OR `links.edit_all` OR admin) and `_can_edit(principal, record)` (owner OR `links.edit_all` OR admin, module-private since only `links.py` needs write-gating) — replacing five duplicated owner-or-admin checks across `handle_list`/`handle_get`/`handle_update`/`handle_delete`/`handle_set_password`. Fixed each 403 body's `required_permission` field to name whichever permission is actually checked at that call site (`links.view_all` for view-gated paths, `links.edit_all` for edit-gated ones) instead of all four hardcoding `links.view_all`.
  - **Pre-existing bug fixed** (found during planning, confirmed in scope): `handle_get`/`handle_update`/`handle_delete`/`handle_set_password` previously gated non-owner access on `principal.role != "admin"` only — none actually checked `links.view_all`, even though `handle_list` already used it to decide list visibility. A `links.view_all` user saw Edit/Delete/View buttons for links they didn't own, and every one of those buttons 403'd. Now fixed via `can_view`.
  - **Second instance of the same bug, found live during verification, not in the original plan**: `api/analytics.py`'s `handle_analytics` and `api/qr.py`'s `handle_qr` had the identical owner-or-admin-only gate — an `editor` user (granted `links.edit_all`) could edit a link's destination but got 403s loading its QR code and click analytics on the detail page (caught via real console-error checking during Playwright verification, not by static review). Fixed by importing and reusing the same `can_view` helper from `links.py` in both files (no cyclic import risk — both already imported `get_link` from `links.py`). Added matching regression tests to `api/tests/test_analytics.py` and `api/tests/test_qr.py`.
  - **GUI** — file(s): gui/admin/users.html, gui/dashboard.html, gui/links/detail.html — done when: added an "Edit all links" checkbox (`links.edit_all`) to `admin/users.html`'s `ALL_PERMISSIONS`/`PERMISSION_LABELS` and both create/edit permission fieldsets. `dashboard.html` now stores the fetched principal (`currentPrincipal`, set by `loadMe()`) and a `canEditLink(link)` helper mirroring the server's `_can_edit`; row-level Edit/Delete (and the paired edit-row markup) only render when the viewer is the link's owner, an admin, or has `links.edit_all` — View/Copy still render for any link visible in the list. Sequenced `loadMe()` before `loadLinks()` (previously concurrent) since `renderLinksTable()` now reads `currentPrincipal` — running them in parallel risked a first-paint race where even the owner's own Edit/Delete buttons could render hidden. `links/detail.html` mirrors the same `canEdit` logic for its Edit button, sequencing `initHeader()` before `loadLinkInfo()` for the same reason.
  - **Regression of my own, found and fixed during verification**: `links/detail.html`'s Copy/Edit buttons defaulted to visible, only getting hidden inside `loadLinkInfo()`'s success path — a user who can't even view the link (analytics.py/`handle_get` 403s) hit the function's early return before reaching that hiding logic, so Copy/Edit stayed visible on an error page for a link the viewer has zero access to. Fixed by defaulting both buttons to `display: none` in the markup and only revealing Copy unconditionally (reaching that line already implies `can_view` passed) and Edit conditionally on the success path — fail-closed instead of fail-open.
  - **Verified live via Playwright**, not just via the test suite: created three real non-admin test users (`links.edit_all` only, `links.view_all` only, no permissions) against a link owned by `admin`, logged in as each, and confirmed: the `edit_all` user sees "All links", full View/Copy/Edit/Delete, can actually PATCH the link via the real API, and the detail page's QR/analytics/Edit all load with zero console errors; the `view_all`-only user sees "All links" and the link with View/Copy only (Edit/Delete correctly hidden), and the detail page loads fully (QR, analytics, Details) with Edit hidden; the no-permissions user sees "Your links" with the link absent entirely, and direct navigation to its detail page shows the existing friendly 403 error with the button group empty (no dangling Copy/Edit).
  - `uv run pytest` — 131 tests (9 new in `test_links.py`, 2 new each in `test_analytics.py`/`test_qr.py`) — and `go test ./linkgate/...` both pass. `detect.mjs --json gui/` unchanged (same 2 known false positives).

## Impeccable critique (7th pass, 30/40) + 2 P1 fixes

- [x] `/impeccable critique gui/` — done when: scored 30/40, the highest of the 7 passes this session. Two confirmed P1 gaps, both in `admin/users.html`'s Users table: (1) the viewer's own row still showed a Delete button even though the server unconditionally rejects self-delete with `cannot_delete_self` — clicking through the full "are you sure, this can't be undone" ritual for a guaranteed-doomed action is actively misleading; (2) promoting a user to `admin` via the role `<select>` + Save had zero confirmation step, despite being the single most consequential action on the page (admins bypass every permission check) — it rode through on the same plain Save as every cosmetic field (disabled toggle, permission checkboxes).
- [x] Fix the 2 P1 confirmation gaps — file(s): gui/admin/users.html — done when:
  - **Self-delete Delete button hidden**: added `currentPrincipal` (populated from `initHeader()`'s `/auth/me` result, sequenced before the first `loadUsers()` call — same pattern as `dashboard.html`'s `canEditLink`). The per-row template now omits the Delete button entirely when `user.username === currentPrincipal.username`, leaving only Edit. Verified live: the admin's own row shows exactly one button ("Edit user admin"), no Delete.
  - **Admin-promotion confirmation**: added `data-original-role="${user.role}"` to the edit-row `<form>`. The submit handler now checks `newRole === "admin" && form.dataset.originalRole !== "admin"` and, if true, routes through the existing shared `confirmDialog()` (`Make "{username}" an admin? Admins bypass every permission check.`, `confirmLabel: "Make admin"`) before issuing the PATCH; Cancel aborts with no request sent. Verified live end-to-end with a fresh test user (`jsmith`): dialog appeared with correct text and the established de-emphasized-destructive-action styling; Cancel correctly left the role unchanged; Confirm correctly PATCHed the role to `admin`; re-saving an already-admin user while only toggling Disabled did **not** re-trigger the dialog (role unchanged before/after — confirmed no false positive).
  - Verified end-to-end: `detect.mjs --json gui/` unchanged (same 2 known false positives). `uv run pytest` (131, unchanged — GUI-only change) and `go test ./linkgate/...` both still pass.
- [x] Fix the 7th critique's remaining 2 P2s + 1 P3 — raised 2026-07-24 — file(s): gui/dashboard.html, gui/admin/users.html, DESIGN.md, .impeccable/design.json — done when:
  - **P2 — row actions clipped outside the visible table** (confirmed at both 1400px desktop and 390px mobile, corroborated by the detector's `nested-cards`/`edge-flush-cards` findings): made the row-action column (last `<td>`/`<th>`, scoped to non-edit rows the same way the existing `nowrap` rule already was) `position: sticky; right: 0` within its scrolling `<figure>`, with a solid `--pico-card-background-color` background and a `--pico-muted-border-color` left border so scrolled content doesn't show through. Applied to both `#links-table` (dashboard.html) and `#users-table` (admin/users.html) — the critique's finding named the links table specifically, but live measurement showed `#users-table` overflows identically at 390px once a user has several permissions (comma-joined labels wrap and widen the column). Verified live: with a 1400px-wide-scrolling links table (a link with a long real-world destination URL, still overflowing 1176px even after the earlier `.destination-cell` truncation fix) and a 390px users table (~3.6× scroll width, matching the critique's own measurement), the Delete/Edit buttons are reachable at scroll position 0 in every case — confirmed via `getBoundingClientRect()` comparison against the figure's visible bounds, not just visually. Screenshot-verified the sticky column visually separates from scrolled content via the border, with no overlap/z-index artifacts.
  - **P2 — user creation has no completion payoff** (violated the app's own documented "give a visible payoff" rule that link creation already follows): added a `#create-success` `.form-success` element to `admin/users.html`, shown as `User "{username}" created.` after a successful `POST /users`, mirroring the link-creation flow's success message. Verified live: created a real test user, confirmed the green success message appeared with the correct username and the create form cleared.
  - **P3 — no progressive disclosure on the primary create-link form** (Starts/Expires/custom-slug/password rendered unconditionally for a "paste URL, submit" modal action): wrapped everything but Destination URL in a native `<details id="advanced-options"><summary>More options</summary>...</details>` on `dashboard.html` — Pico styles `<details>`/`<summary>` natively (chevron marker, color-shift), so this needed zero custom CSS, consistent with the app's established preference for native platform idioms (`<dialog>` for confirmations) over bespoke JS toggles. Resets to closed after each successful create (`advanced-options.open = false`), matching the existing full-field-reset behavior. Verified live end-to-end at both viewport sizes: closed by default: only Destination URL + collapsed summary visible; clicking expands with a smooth native chevron rotation; created a link with the panel open and Starts/password filled in — the API payload, success message, and post-create collapse all worked correctly.
  - Updated `DESIGN.md` (new "Sticky action column" and "Progressive disclosure" entries under Layout/Components, 2 new Do's, and a note that the payoff rule applies to every completing flow) and refreshed the `.impeccable/design.json` sidecar (`Scrollable Data Table` component's description/CSS extended for the sticky column; new `Progressive Disclosure (Details)` component; matching `dos` entries).
  - Regression-verified the surrounding functionality wasn't disturbed: dashboard sort, Copy, Edit-toggle, Cancel, and a full Delete-with-confirm-dialog round-trip all still worked through the sticky-column layout change, zero console errors throughout.
  - `detect.mjs --json gui/` unchanged (same 2 known false positives). `uv run pytest` (131, unchanged — GUI-only) and `go test ./linkgate/...` both still pass.
